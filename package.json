{
  "name": "@proai/canvas-receipts",
  "version": "1.0.1",
  "description": "",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "esbuild src/index.js --format=esm --platform=browser --bundle --outdir=dist",
    "version:patch": "npm version patch",
    "version:minor": "npm version minor",
    "version:major": "npm version major",
    "prepublishOnly": "npm run build",
    "publish:patch": "npm run version:patch && npm publish",
    "publish:minor": "npm run version:minor && npm publish",
    "publish:major": "npm run version:major && npm publish"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "canvas": "^3.1.0",
    "d3": "^7.9.0",
    "date-fns": "^4.1.0",
    "node-fetch": "^3.3.2",
    "numeral": "^2.0.6",
    "plotset-formatter": "^1.2.150",
    "uglifyjs": "^2.4.11"
  },
  "publishConfig": {
    "@proai:registry": "https://gitlab.pro.ai/api/v4/projects/715/packages/npm/"
  },
  "files": [
    "dist",
    "index.d.ts",
    "types"
  ],
  "devDependencies": {
    "esbuild": "0.23.1"
  }
}
